# Scenario TODO / Статус реализации

Снимок состояния: **27 июля 2026**
Назначение файла: единая точка контроля по сетевым и игровым задачам в `Assets/_Scripts`.

См. также:
- `Assets/Igrodelnya2.0/Localization/Reports/Localization_TODO.md` (локализация вынесена отдельно)
- `Assets/Igrodelnya2.0/Localization/Reports/LocalizationRows_Proposed_UI.csv`
- Серверный проект: `/Users/Roman/zoogame-backend`

## Легенда стадий

- **Не делалось** — задачи не начинались.
- **Есть код** — базовая логика добавлена, но не доведена в игре.
- **В процессе** — часть сценариев работает, есть заметные пробелы.
- **Нужна настройка в Unity** — код есть, но не настроены ссылки/префабы/сцены.
- **На тесты** — по коду и настройке готово, нужен прогон кейсов.
- **Протестировано** — базовые кейсы проверены, возможны мелкие правки.
- **Готово** — принято, можно считать рабочим.

---

## 1) Онлайн-лобби и синхронизация

| Фича | Статус | Готовность | Комментарий |
|---|---|---:|---|
| Подбор лобби при входе + fallback в оффлайн | **Протестировано** | 97% | Добавлен отдельный capacity-fallback: сервер возвращает `429 capacity_degraded`/`Retry-After`, клиент выключает realtime без блокировки локальной игры, один раз подставляет сохранённые снимки чужих баз и повторяет вход с jitter. Live Bridge подтвердил переход `CapacitySnapshot -> Online` без перезапуска и загрузку 5 публичных snapshots. Обычный сетевой сбой по-прежнему оставляет только локальную базу. |
| Закрепление слота игрока за сервером | **В процессе** | 80% | Слот выдается сервером и обновлен на случайный при новом входе; осталось проверить edge-cases rejoin в боевых тестах. |
| Отображение удаленных игроков (движение/анимация/направление) | **В процессе** | 90% | Разделена логика: culling баз по дистанции не выключает remote-player. Клиент больше не переопределяет серверный `isOnline` по `updatedAt` и часам устройства — этот ложный stale-фильтр скрывал реально присутствующих участников. Live Play Mode подтвердил получение двух участников одного лобби и активный remote-player со всеми включёнными рендерерами. Визуальные модели локального и удалённого игрока синхронно опущены на `0.08` юнита относительно физического контроллера; осталось добить edge-case rejoin/таймаутов в двухклиентном smoke. |
| Синхронизация предмета в руках (яйцо/животное/фрукт) | **В процессе** | 82% | Серверный `LobbyHandItem` расширен: `element/weight/income` теперь доходят до remote-клиента; осталось визуально подтвердить все типы и редкости в боевом smoke. |
| Фильтрация дальних баз/игроков | **На тесты** | 70% | Дальность увеличена, но нужен стабильный сценарий первичной загрузки без пустых баз. |
| Безопасный выход из лобби при дисконнекте | **В процессе** | 88% | Усилен cleanup: hard-reset remote slots/players/snapshot state в offline; stale-offline задержка повышена до 30 сек (клиент), на сервере уже 30 сек; добавлен принудительный reload локального слота из сейва при смене ownership после reconnect; нужен повторный smoke на join/rejoin/disconnect/NET OFF. |

---

## 2) Взаимодействие игрок-игрок

| Фича | Статус | Готовность | Комментарий |
|---|---|---:|---|
| Предложение дружбы при прицеливании в игрока | **В процессе** | 84% | Усилен hold-flow и авто-refresh панели друзей после внешних действий; добавлен анти-срыв удержания в конце прогресса (`inputDropGrace + nearCompleteThreshold`), добавлен resume hold при кратком `InteractionPanel` disable/enable и защита `RemoteFriendBoard` от скрытия панели во время активного hold; нужен повторный soak на длинную сессию. |
| UI входящих заявок в друзья | **В процессе** | 60% | Попап/поток добавлен, требуется довести список друзей и online-state. |
| Подарок другому игроку (яйцо/животное/фрукт) | **На тесты** | 90% | Добавлены авто-accept возвратных подарков (без отдельного попапа), fallback отправки по `friendCode` и фиксы hold-срыва; нужен smoke 2 клиента/3 типа предметов. |
| Блокировка лишних действий по одному нажатию E | **Есть код** | 55% | Нужен единый арбитр приоритетов между raycast-целями. |

---

## 3) База и геймплей на слоте

| Фича | Статус | Готовность | Комментарий |
|---|---|---:|---|
| Локальная база всегда интерактивна только в своем слоте | **В процессе** | 95% | Добавлен принудительный restore local-slot из сейва при смене server-slot (перезагрузка `FieldManager`, возврат `Conveyor/BigPet` в local-режим), убрана destructive-запись `FieldUnblock=false` при `Field.Init`; серверный `baseData` больше не применяется к локальному сейву (локальный save авторитетен через Mirra SDK). `RemoteBasesApplier` ждет `SaveManager.IsReady` перед restore, а кеш `playerId` стабилизирован на bootstrap. После регрессии откатан fallback-local-slot; загрузка ячеек выполняется только для разблокированных полей (для заблокированных — только очистка runtime-актеров), чтобы убрать ошибки на неактивных объектах и вернуть работу молотка. Дополнительно `ZooBaseSnapshotSync` теперь включает default-open поля в `land.boughtCells` и не отправляет snapshot до готовности save/резолва local-slot. В `RemoteBasesApplier` добавлен guard: root для snapshot доступен только после server-resolve + успешного локального restore; а в offline отключен лишний restore для уже подготовленного локального слота (чтобы `NET OFF/ON` не откатывал базу). Нужен повторный smoke reconnect 30+ сек. |
| Удаленные базы только визуальные (без лишнего UI) | **В процессе** | 84% | Пустые удалённые слоты теперь обязательно получают кешированный случайный fallback и остаются видимыми как в online, так и в offline; реальный snapshot игрока всегда заменяет фальшивый прогресс. Две именно удалённые локации являются развитыми витринами: 88–100% территории, 15–22 занятых ячейки из верхних 40% питомцев, усиленные стихии/вес, максимальный или предмаксимальный конвейер и купленный elemental BigPet. Обычные животные после вылупления, ручной установки, загрузки сохранения или remote/fallback spawn выравниваются по нижней границе Mesh/SkinnedMesh модели и поверхности клетки, поэтому различия pivot и масштаба больше не заставляют их висеть или проваливаться. Bridge при одном online-игроке и в принудительном offline подтвердил пять активных remote-баз: обычные `2/4/4` животных, showcase `15/15`, открыто `16–41` полей; отдельный свежий прогон подтвердил `37/37` животных с отклонением не более `0,0106` юнита во время анимации. Нужно отдельно закончить аудит отключения UI на всех remote-точках. |
| Большой питомец покупается и открывается по условию | **На тесты** | 98% | Сервер+клиент подключены. После всех 18 обычных животных прогрессия продолжается улучшенными вариантами `Gold`, `Diamond`, `Electric`, `Fire`; flat variant ID хранится в прежнем `SaveBigPetId`, поэтому старые сохранения остаются совместимыми. Elemental BigPet получает реальный URP tint, VFX и множитель дохода стихии. Площадка выбора использует collider по visual bounds (`3.16 × 0.86 × 3.16`) и небольшой close-buffer вместо старой огромной зоны. После регрессии короткого downward-ray открытие переведено на переход пересечения компактной площадки с `CharacterController.bounds`: в центре окно автоматически открывается, после выхода закрывается, а ручное закрытие внутри не вызывает постоянного переоткрытия. Окно остаётся узким вертикальным: зелёная шапка/бонус/квадратный крестик, категории «Обычные/Улучшенные», пять фильтров стихий с замками и двухколоночные Album-like карточки. Bridge подтвердил `open=True` в центре и `open=False` после отхода на 3 м. Остались пользовательский визуальный smoke и полный цикл покупки/кормления на чистом профиле. |
| Конвейер и яйца на чужих базах | **В процессе** | 70% | Исправлен forced-selection баг Tutorial шага 10: `ShowLevel(NextUpgradeLevel)` вызывается только при первом открытии окна, поэтому ручное переключение уровней сохраняется (`first=next`, повторный refresh оставил выбранный `level 0`). Для remote-баз по-прежнему нужны полные правила появления/исчезновения и покупок. |

## 3.1) UI/UX попапы

| Фича | Статус | Готовность | Комментарий |
|---|---|---:|---|
| Универсальный popup подтверждения (`YenOrNot`) | **В процессе** | 92% | `UniversalDecisionPopup` подключен к `GiftInboxUI` и `FriendRequestInboxUI`, а поиск popup в `FriendsPanelController` расширен до `GameCanvas` (включая sibling-расположение `YenOrNot`); осталось перевести на него остальные confirm-сценарии и прогнать UX-smoke. |
| Профиль другого игрока (`RemoteProfilePopup`) | **На тесты** | 98% | Popup использует общую `texture` для всех панелей и кнопок, двойную жирную чёрную обводку текста, `Gradient2` на кнопках и Russo One для всех надписей; крестик принудительно сохраняет квадрат `68 × 68`. Подключён к live RU/EN локализации и при открытии монтируется в активный `GameCanvas`. Для владельца таблички добавлены карандаш с inline-сменой ника и WebGL-safe копирование friend code; на чужой табличке owner-действия скрыты. Bridge визуально подтвердил базовый popup, читаемый текст, квадратный крестик и кнопку лайка; остался smoke новых owner-кнопок и двухклиентный smoke лайка/сетевых данных. |

---

## 4) Приоритетный рабочий план (ближайшее)

### P0 — блокеры
- [~] Tutorial V2: утверждённые 10 самостоятельных заданий реализованы — per-step `stable_id` save/migration, старые игроки, rename/reorder safety, context scheduler, прижатая к правому краю полупрозрачная чёрная панель с выездом вправо, анимированная рука для UI и world-стрелка, completion-анимация, бесплатное первое/rewarded последующее ускорение яйца и плавный ранний/резко усиливающийся поздний бонус дохода BigPet до `+5000%` (`x51`) на 450-м уровне. Панель приведена к общему UI-стилю: мягкая stud-текстура, зелёная градиентная шапка, тёмная подложка задания, общие RussoOne/outline, оранжевая кнопка сворачивания и зелёная claim/continue-кнопка с отдельными disabled/ready состояниями. Цель и получение награды разделены: после выполнения кнопка `Забрать +N`/`Продолжить` активируется, а награда `0/3/3/5/0/0/5/3/3/10` (всего `32`) выдаётся только по нажатию и защищена от дублей/перезапуска; skip отсутствует. Unity compile, validator на 10 шагов/66 RU-EN ключей и live Bridge spot-check пройдены. Остались полные clean-profile offline/online/touch прогоны с restart/reward/ad проверками. Спецификация: `Docs/TUTORIAL_V2_BACKLOG.md`.
  - [x] 21.07.2026: восстановлен случайно отключённый `GameCanvas`; live Play Mode подтвердил активные HUD, currency, quick slots, меню, магазин и альбом. TutorialView выровнен по общей трёхзонной сетке, нижняя claim/continue-кнопка растянута на внутреннюю ширину, а кнопка сворачивания совмещена с правым краем и высотой шапки.
  - [x] 21.07.2026: устранён ложный переход сразу к шагам 6–7. Исторические животные, открытые территории, улучшения и записи альбома больше не доказывают покупку/установку/вылупление учебного яйца; цепочка 2–6 опирается на отдельные сохранённые gameplay-факты и локальные сигналы. Добавлена одноразовая починка уже повреждённых tutorial-save: неверные шаги открываются заново, но ранее выданные награды сохраняются и повторно не начисляются. Live-профиль восстановился с `buy_big_pet` на `buy_first_egg` при `0` яиц в инвентаре/клетках.
  - [x] 21.07.2026: мировые цели Tutorial V2 переведены с экранной стрелки на анимированную жёлтую дорожку `ArrowLine` по земле; UI-цели показывают только `UIHandPointer`. Исправлены нерабочий player-start в `AutoArrowLine`, URP transparent material, ground projection, back-face culling и неверный Editor safe area, из-за которого рука иногда уходила за экран. Live Bridge подтвердил жёлтую world-дорожку и руку над UI без чёрной замены.
  - [x] 21.07.2026: дальняя world-дорожка больше не растягивается до цели: видимый участок ограничен ближайшими `14` метрами, семплируется по земле, а `Tile`-текстура больше не получает повторное масштабирование по полной длине, поэтому не превращается в частые полосы. На шаге 10 до накопления динамической цены улучшения (`15K` для первого) стрелки и подсветки нет; после накопления цель меняется на реальную зону `LevelsArea`, открывающую окно конвейера. Live Bridge подтвердил `lowTarget=null`, `readyTarget=LevelsArea` и ограниченный участок `12.9` м при цели в `60` м.
  - [x] 22.07.2026: на шаге 7 во время накопления динамической цены BigPet скрыты обе стрелки и подсветка; после достижения цены сохраняется прежняя контекстная цепочка «дом → место BigPet → кнопка покупки». Completion-анимация больше не масштабирует весь `TutorialPanel` за границы `RectMask2D`: безопасно появляется только кнопка награды в диапазоне `0.9..1.0`. Bridge подтвердил `lowPrimary/secondary/highlight=null`, неподвижный scale панели `1.0` и восстановление scale награды после завершения/отключения.
  - [x] 04.08.2026: шаг 8 полностью переписан вокруг конечной цели «купить фрукт и покормить большое животное»: русская подсказка больше не показывает `FOOD`, каждый контекст ведёт к кормлению, а финальный текст явно сообщает, что именно кормление завершает задание. Локализация теперь создаётся до loading UI, а Tutorial дополнительно реагирует на fallback-смену языка; остался визуальный smoke загрузки и шага 8 в Play Mode/WebGL.
  - [x] 04.08.2026: круг входа в магазин еды сдвинут ближе к прилавку и слегка поднят; raycast-коллайдер расширен до полного диаметра визуального круга и выведен над полом, чтобы вся отмеченная зона стабильно открывала магазин.
- [~] Стабилизировать стартовый loading screen: он по-прежнему ждёт готовый save, завершённый initial join, восстановленный локальный слот и фактический телепорт игрока. Исправлены три startup-race: backend guest ждёт готовности Mirra-save, появившийся позже `RemoteBasesApplier` применяет закешированный initial lobby state, а `GameEntryPoint` отличает runtime-компонент сцены от назначенного prefab-asset. Проверка готовности повторяется и показывает диагностическую причину ожидания. После принудительного Unity-import чистый старт через `LoadingScene` подтвердил `loadingHidden=true / loading_hidden` без ошибок Console; остался контрольный старт platform WebGL.
- [x] Убрать фантомного клона локального игрока в любых сценариях join/rejoin/offline (по последним тестам проблема не воспроизводится).
- [x] Довести поток подарков до полного цикла: отправка -> уведомление -> принять/отклонить -> корректный результат (прогон на 2 клиента пройден, критичных проблем не найдено).
- [x] Починить стабильный показ кнопок "Добавить в друзья" / "Подарить" при обычном наведении (повторные тесты пройдены, поведение стабильно).
- [x] Исправить разъезжающиеся тексты в UI друзей (исправлено, подтверждено ручным прогоном).
- [x] Починить копирование ID в UI друзей для WebGL и сброс состояния «Скопировано» при закрытии/повторном открытии окна.
- [~] Восстановить заявки и визит к другу: форма ID снова создаёт pending-заявку с явными «Принять/Отклонить», а «В гости» вызывает `join-with-friend` и телепортирует к серверному слоту друга. Остался живой smoke на двух клиентах/WebGL.
- [~] Исправить повторное открытие альбома и cleanup окна общей кассы: кнопка альбома теперь всегда выполняет идемпотентный `Open`, закрытие снимает stale UI-selection; `ClaimAllCoinsZone` принудительно скрывает свой popup/затемнение при выходе, телепорте, отключении зоны и смене локального слота. Изолированная C#-компиляция пройдена, остался Play Mode smoke в `steal_brainrot` (активный Bridge подключён к другому Unity-проекту).
- [~] Закрыть повторную бесплатную крутку рулетки после перезахода: загрузка daily timestamp теперь ждёт готовности save-провайдера, запись форсированно синхронизируется до начала вращения, таймер пересчитывается по реальному UTC, а отмена вращения больше не возвращает бесплатную попытку. Код рулетки компилируется; остался restart-smoke в Editor и WebGL.

### P1 — стабильность сети
- [x] Усилить reconnect-стратегию после `server_unreachable` без ломки локальной базы (ручной прогон online/offline пройден, критичных регрессий не найдено).
- [~] Пилот WebSocket для лобби: серверный `/ws/lobby` + Unity `LobbyClient` (WS с fallback на HTTP polling) готовы. Для WebGL добавлен browser-native `WebSocket` bridge: вместо принудительного HTTP update/state polling раз в секунду транспорт после `ws_ready` отправляет update с интервалом `0,2 с`, при ошибке автоматически возвращается к HTTP. Серверный broadcast-path оптимизирован и развёрнут: одна загрузка friend/meta на всё лобби с 5-секундным кешем, общий snapshot строится один раз, отправки идут параллельно, одинаковые update не повышают version, а `sync_request` отвечает только инициатору. Серверный soak прошёл на 6 клиентах × 100 duplicate + 100 unique update: duplicate ACK p95 `2.17 ms`, полный unique broadcast p95 `8.30 ms`, без лишних broadcast/sync leak/restart/OOM. Сборка и JS/IL2CPP linkage проверены; localhost Mirra не выдал player profile, поэтому остался живой smoke двух platform-WebGL клиентов и reconnect/длинная сессия.
- [x] При уходе в offline гарантированно убирать remote-сущности и временный state (прогон подтвержден).
- [x] Добавлена debug-галочка симуляции оффлайна (`NET OFF`) для ручных тестов reconnect/offline UX (прогон подтвержден).
- [x] Добавлен grace-period для отметки remote-игрока оффлайн: `remoteMemberStaleOfflineSec >= 30s` (клиентский clamp + дефолт); на текущем smoke пропаданий не выявлено.
- [x] Добавить финальный smoke-набор для 1/2 игроков в одном лобби (прогон выполнен, критичных проблем не найдено).
- [x] Soak 10-20 игроков: проверка распределения по лобби + `join-with-friend` (проведен exact synthetic `user-flow`: `6/4/2 -> 5/4/3 -> 6/6/4/1 -> 5/5/6/1`, затем новые входы `-> 6/6/6/2`; подтверждены кейсы `join-with` при свободном месте и при полном лобби с переносом пары).
- [~] Защита вместимости сервера: soft/hard cap, 5% серверного premium-резерва, автоматический admission-stop при устойчивой CPU/RAM нагрузке, ops-status и внешний Telegram/SMTP watchdog развёрнуты на тестовом backend. Проверены `3 ordinary -> reserve reject -> premium admit -> hard reject`, resource-pressure/recovery и live Unity `CapacitySnapshot -> Online`. Остались реквизиты реального канала уведомлений владельца и привязка premium к проверенному store receipt.

### P2 — функциональные задачи (после стабилизации)
- [~] Звуки GDC 2026 — из локального Sonniss-бандла и одного сохранённого CC0-исходника собрано 88 компактных Ogg-клипов и подключены события через единый `SoundManager`: UI, движение, взаимодействия, экономика, яйца/вылупление, BigPet, территория, конвейер, альбом, магазин, рулетка, Tutorial V2, социальные действия, сеть и окружение. По listening pass обычный прыжок сделан тихим, шаги переведены на Animation Events с Animator ×2/Shift ×3 и расширенным random pitch, последовательный сбор монет получил плавный rising-pitch combo на 20 шагов с reset через 1 секунду, для монет выбран короткий CC0 `Coin Splash` без reward-колокола, для кристаллов — `Button Arp Twinkle`, common mob reveal заменён на cute bird cue, а для плавания выбран `water_move_04`. Для WebGL 82 коротких эффекта загружаются как Compressed In Memory, 6 длинных атмосфер стримятся в фоне. Остались финальный слуховой баланс и повтор критических действий в WebGL. Документация: `Docs/AUDIO_GDC2026_INTEGRATION.md`.
- [x] Перевести first-party UI с legacy `UnityEngine.UI.Text` на TextMesh Pro — мигрированы игровые сцены, префабы, runtime/editor-поля и генератор Adaptive Grid demo; сохранены сериализованные ссылки и локализация, все игровые TMP-компоненты унифицированы на `RussoOne-Regular SDF` с существующим Cyrillic fallback. Для пяти world-space подписей яйца и четырёх подсказок взаимодействия восстановлен отдельный `RussoOne-Regular SDF Overlay`, чтобы их не перекрывала геометрия. Legacy-компоненты остались только в сторонних примерах VoxelImporter и recovery-сцене.
- [x] Мягкая текстура UI — принятый пилот `UIWindowTexture_SoftStuds_v1` расширен со SpecialShop на все first-party окна, панели, инвентари и создаваемые карточки. Заменено 135 дополнительных сериализованных ссылок; 3D-материалы мира и служебная recovery-сцена намеренно оставлены на исходной текстуре. После перезагрузки сцены live-аудит показал `0` старых / `434` новых UI-изображения; отдельно визуально проверены магазин, инвентарь, альбом и конвейер.
- [~] Normal map старой 3D-текстуры — создан повторяемый `texture_Normal` через Unity normal-from-height (`heightScale 0.12`, mipmaps, repeat). Высотная маска инвертирована, чтобы LEGO-элементы читались выпуклыми, а не вдавленными. Карта подключена как `_BumpMap` к двум материалам, где старая текстура является основным цветом, и заменила некорректную цветную `texture.png` в `_DetailNormalMap` четырёх материалов окружения. Live Bridge подтвердил рельеф на дорожках и бордюрах; осталось решение по итоговой силе после пользовательской визуальной оценки.
- [~] Плавность камеры — орбитальный ввод ограничен по выбросам и длинным кадрам, целевые yaw/pitch сглаживаются через `SmoothDampAngle`, первый delta после pointer lock игнорируется, возврат после коллизии использует стабильный unscaled timestep. Компиляция пройдена; остался ручной smoke мыши/touch в Editor и WebGL на слабом устройстве.
- [~] Производительность сцены — устранён периодический спайк `LocalProfileBoardPoint.Update` (~239 мс → максимум 0,037 мс): полная статистика фермы теперь считается только при открытии профиля, а сетевой snapshot кеширует статическую иерархию и не перечитывает животных второй раз. Bridge-профиль после правок: `PlayerLoop` median `7,71 мс`, p90 `9,98 мс`. Эксперимент с полным выключением пустых remote-баз откатан по продуктовому решению: острова всегда должны оставаться видимыми с fallback-прогрессом, поэтому следующая безопасная оптимизация для них — lazy/LOD-прокси. Остальной WebGL pass ограничивает tutorial hand до 512px (`6 295 440 -> 263 716` packed bytes), исключает Google API/credentials из player и включает stripping неиспользуемого post-processing. Финальный audit build успешен (`0 errors`, `27 313 003` bytes whole folder); с учётом новых 88 Ogg-клипов сопоставимое ядро стало меньше на `110 621` bytes. Сейчас шесть слотов суммарно держат около `41 700` объектов, `5 400` Canvas и `9 500` TMP-компонентов, поэтому Editor domain reload и слабый WebGL остаются тяжелее необходимого. Полный аудит: `Docs/WEBGL_OPTIMIZATION_AUDIT_2026-07-28.md`.
- [~] Zone "Claim all coins" + апгрейд на сбор без рекламы (popup flow и автосбор после unlock готовы; scoped key reset стабилизирован через `Tools/Save/Clear ALL Save Data`). Осталось: smoke в живой сцене и точечная проверка привязок префаба (popup/indicator/audio).
- [~] Шансы животных в яйцах — каталог конвейера показывает каждое уникальное яйцо, его luck и животных внутри с реальными шансами вылупления; вероятности выпадения самих яиц и группировка по уровням конвейера удалены. Заголовки яиц окрашены по редкости, строки животных чередуются, RU/EN заголовок и краткая аннотация соответствуют содержимому. Основное окно переведено с фиксированного смещения на адаптивные якоря. Иконки яйца/животных получили альбомные карточки без имён с мягкой текстурой, градиентом и обводкой, а заголовки и строка дохода — отдельные тёмные подложки. Bridge подтвердил 8 яиц по 4 животных, сумму `100%` для каждого яйца, отсутствие строк конвейеров, soft-stud sprite и карточки `97.59 × 97.59`. Остался финальный визуальный smoke полного списка в целевом WebGL-билде.
- [~] Баланс яиц, конвейера и удача игрока — текущий V1 подтвержден без дополнительных двух конвейеров. Базовый шанс `NoElement` выставлен в 99%, `PlayerLuckHub` повышает шанс элементального яйца от последнего купленного уровня конвейера; яйца `EGG_1..EGG_8` переведены на overlap-прогрессию с weighted slots `55/25/15/5`, основные животные получили новую CPS-лестницу, уровни конвейера настроены на progression weights, а цены/бонусы апгрейдов подняты до долгой кривой (`+100%..+1000%` дохода, `+50%..+500%` element luck). Прогрессия с 1-го по 4-й конвейер оставлена быстрой, после Epic поздние яйца и апгрейды подняты примерно в late x60, чтобы растянуть хвост прохождения. Осталось: Unity-smoke выпадений/иконок/бонусов и решение по поздним неиспользуемым яйцам.
- [~] Баланс разблокировки травы — `FieldManager` считает цену по расстоянию от стартовой открытой зоны: базовая цена `900`, множитель кольца `2.9`, округление до `100`; дальние кольца с 5-го дополнительно умножаются на `60`, чтобы позднее расширение базы стоило примерно как средне-поздние яйца. Осталось: Unity-smoke на текущей сетке и ручная подстройка кривой после теста темпа расширения.
- [~] Вода на сцене — `Water` в `Evgesha.unity` получил WebGL-friendly animated material (`Water_WebGL`), `WaterSurface` для bounds/высоты и `WaterFloatRider` на игроке для всплытия/прыжка от стенки; MeshCollider воды отключен, чтобы игрок не вставал на поверхность. Осталось: play-mode smoke падения в воду, прыжка у стенки и WebGL визуала.
- [~] Табличка профиля у базы + расширенная статистика игрока — `LocalProfileBoardPoint` работает по владельцу слота, скрывает board без активного игрока, передает `playerId/friendCode` и owner-state в popup. `RemoteProfilePopup` локализован, использует общие texture/outline/gradient/font assets и надёжно размещается на активном `GameCanvas`; для своей таблички доступны inline-смена ника через карандаш и копирование кода, а кэш локального слота больше не откатывает новое имя. Осталось: визуальный smoke owner-кнопок, двухклиентный smoke данных/лайка, `Slot Index Override` и финальная расстановка.
- [~] Лайки 1 раз в день (с серверным ограничением) — клиентский popup/кнопка готовы; добавлен tolerant parsing payload даже при non-2xx ответах `/likes/*` (чтобы корректно показывать `likedToday`/таймер). Осталось: интеграционный smoke-тест клиент+сервер.
- [~] Альбом питомцев и яиц (отдельный большой этап) — добавлен фундамент: `AlbumProgressService` (unlock/save/mention/reward flags), `AlbumScreenController` + базовые view-компоненты карточек/rare-вкладок, интеграция с `Inventory` и `QuickAccess` для автo-открытия при получении/удержании предметов; усилена иерархия mention (карточка учитывает `card+reward+rare`). Добавлен гайд настройки: `Docs/ALBUM_SETUP.md`. Дополнительно выполнен hardening: rare-mention переведен на уровень сущности (вместо tab-wide), добавлена локализация подписей редкостей, добавлены guard'ы поздней инициализации каталога/инвентаря. Осталось: привязка рефового UI в сцене, финальная визуальная логика силуэтов/анимаций mention и UX smoke.
- [~] Магазин V2 — возвращён к единой длинной ленте: верхние пункты работают как ссылки с плавной прокруткой к секциям, а не как фильтрующие вкладки. `Навсегда` временно скрыта, три оставшиеся ссылки растянуты поровну. Карточки сохраняют упрощённый состав и адаптивную раскладку. Добавлен безопасный Editor-only preview режимов платформы: ad-only показывает один набор `+5`, IAP — оба кристальных набора с тестовой provider-ценой и без выполнения покупки. Live Bridge проверил секции/якоря, `7` ad-only и `8` IAP карточек при `1280×720`. Осталось завести платформенные SKU для стартового набора и отключения рекламы, а также проверить восстановление покупок в реальном Yandex build.
- [ ] Сохранение наград за время (`L`) — запоминать полученные слоты между сессиями и исключить повторный сбор после перезапуска.
- [ ] Закрыть предупреждения локализации животных (`Item/Alpaca`, `Item/Sheep`, `Item/Cat` и остальные ключи из Unity Console).
- [~] Мировые лидерборды и донаты — клиентская запись/чтение пяти таблиц переведена на Mirra Achievements без собственного backend; подключены заработок, недельные/all-time вылупления, лучший питомец месяца и donation SKU, локальный cache/Editor fallback, RU/EN и базовые мировые стенды. Нужны настройка пяти ID/периодов и SKU в кабинете Mirra, а также визуальный Play Mode/WebGL smoke и финальная расстановка. Полная постановка: `Docs/NEXT_TASKS_HANDOFF.md`, `P2.3`.

---

## 5) Чек-лист регресса перед релизом

- [ ] Новый аккаунт: корректный спавн в своем слоте, без телепорта в чужую базу.
- [x] Два игрока в одном лобби: видимость друг друга, корректное движение, без фантомов.
- [ ] Два игрока в одном лобби по WebSocket: state синхронен, при недоступности WS корректный fallback на HTTP.
- [x] Нагрузка 10+ игроков (синтетический прогон): очередность заполнения лобби соответствует правилам для capacity=6 (`6/4/2`, friend-join перераспределение, затем добор в `1 -> 2 -> 4` лобби при полном `3`); живой массовый тест реальными игроками остается желательным.
- [~] Invite friend: при наличии 1 места в лобби друга игрок входит туда; если мест нет — оба попадают в лобби с 2 свободными слотами или в новое. Серверные synthetic-тесты пройдены, клиентская кнопка подключена к `join-with-friend`; остался живой smoke двух клиентов.
- [x] Подарок: отправка/получение/отклонение работают одинаково для яйца, животного и фрукта; при отказе подарок автоматически возвращается дарителю без отдельного подтверждения.
- [x] Дисконнект сервера: клиент уходит в offline без nullref и без дублей баз.
- [x] Возврат сети: переподключение не ломает локальную интерактивную базу.
- [x] UI друзей: статусы online/offline и тексты отображаются корректно.

---

## 6) Правило ведения файла

- После каждой заметной задачи обновлять статус и процент по строкам выше.
- Новые крупные задачи добавлять сначала в раздел **P2**, затем поднимать в **P1/P0** по приоритету.
- Локализацию не смешивать с этим файлом — вести только в `Localization_TODO.md`.
- Если меняется серверная логика, обязательно:
  1) запушить изменения в `E:\GitFork\zoogame-backend`;
  2) сообщить пользователю команды для применения на прод-сервере.

### Напоминание по деплою сервера

После серверных изменений напомнить пользователю выполнить:

```bash
cd /srv/farmgame
git pull
docker compose stop nginx certbot api
docker compose build api
docker compose up -d
```
