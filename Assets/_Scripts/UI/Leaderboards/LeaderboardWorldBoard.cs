// Unity requires a serializable MonoBehaviour component to live in a script file
// with the same name. The implementation remains in the shared base class so the
// installer and popup can stay together without losing prefab component links.
public sealed class LeaderboardWorldBoard : LeaderboardWorldBoardBase
{
}
